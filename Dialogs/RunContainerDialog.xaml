<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WslcDesktop.Dialogs.RunContainerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border
            x:Name="Backdrop"
            Background="#99000000"
            Tapped="Backdrop_Tapped" />

        <Border
            MinWidth="520"
            MaxWidth="640"
            MaxHeight="640"
            Padding="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{ThemeResource ContentDialogBackground}"
            BorderBrush="{ThemeResource ContentDialogBorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Tapped="DialogCard_Tapped">
            <Grid RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollViewer
                    HorizontalScrollMode="Disabled"
                    VerticalScrollBarVisibility="Auto"
                    VerticalScrollMode="Auto">
                    <StackPanel MinWidth="480" Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon
                                VerticalAlignment="Top"
                                FontSize="28"
                                Foreground="{StaticResource AppAccentBrush}"
                                Glyph="&#xE7B8;" />
                            <StackPanel Spacing="2">
                                <TextBlock
                                    FontSize="20"
                                    FontWeight="SemiBold"
                                    Text="Run a new container" />
                                <TextBlock
                                    x:Name="ImageNameText"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Padding="0,8,0,0" Spacing="6">
                            <TextBox
                                x:Name="ContainerNameBox"
                                PlaceholderText="Container name" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="A random name is generated if you do not provide one." />
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock FontWeight="SemiBold" Text="Ports" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Container ports come from the image. Enter &quot;0&quot; to assign randomly generated host ports." />
                            <StackPanel x:Name="PortsPanel" Spacing="8" />
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock FontWeight="SemiBold" Text="Volumes" />
                            <StackPanel x:Name="VolumesPanel" Spacing="8" />
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock FontWeight="SemiBold" Text="Environment variables" />
                            <StackPanel x:Name="EnvVarsPanel" Spacing="8" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button
                        x:Name="CancelButton"
                        MinWidth="88"
                        Click="CancelButton_Click"
                        Content="Cancel" />
                    <Button
                        x:Name="RunButton"
                        MinWidth="88"
                        Click="RunButton_Click"
                        Content="Run"
                        Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
